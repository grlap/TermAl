# Bugs & Known Issues

Updated against the current checked-in code in `src/main.rs`, `ui/src/App.tsx`,
`ui/package.json`, and `ui/vite.config.ts`.

Completed items are removed from this file once fixed. There are no active bugs or
follow-up tasks currently tracked here.